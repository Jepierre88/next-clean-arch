
# -----------------------------
# Better Auth (required)
# -----------------------------
# Secret used to encrypt/sign session cookies (min 32 chars).
# Generate one:
#   openssl rand -base64 32
# or
#   npx @better-auth/cli secret
BETTER_AUTH_SECRET="replace-with-a-32+char-random-secret"

# Base URL of your app (used for redirects/callbacks).
# For local dev:
BETTER_AUTH_URL="http://localhost:3000"

# -----------------------------
# Backend API (required for credentials login flow)
# -----------------------------
# Used by server-side datasource to call your backend login endpoint.
BACKEND_URL="http://localhost:3001"

# Optional: expose backend url to the browser if you also call it from client-side code.
# (The code uses BACKEND_URL first, then NEXT_PUBLIC_BACKEND_URL)
NEXT_PUBLIC_BACKEND_URL="http://localhost:3001"
